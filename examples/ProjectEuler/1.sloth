func solve(num) {
    // Find the sum of all the multiples of 3 or 5 below 1000.

    var sum = var i = 0;

    while i < num {
        (i % 3 == 0 || i % 5 == 0) && (sum += i);
        i += 1;
    }

    return sum;
}

println(solve(1000));